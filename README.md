# Big Number Calculator — Angular

Лабораторная работа: веб‑приложение‑калькулятор для работы с большими числами (до 1 триллиона).

## Описание

Приложение позволяет вводить формулы из **четырёх операндов** и рассчитывать результат с учётом приоритета операций, поддержки скобочной логики для средних операндов и разных способов округления. Проект написан на **Angular** и предназначен для демонстрации работы с валидацией ввода, аккуратными вычислениями и таблицированным отображением данных.

---

## 📋 Функционал

- **4 операнда** (формула вида `A op1 B op2 C op3 D`).
- Приоритет операций:
  - стандартный приоритет умножения/деления;
  - логика «скобок» для средних операндов (если реализована в UI).
- **Строгая валидация** ввода: поддержка пробелов как разделителей тысяч, запрет некорректных символов.
- Работа с числами в диапазоне **±1 000 000 000 000** с точностью до **10 знаков** в промежуточных вычислениях.
- Три режима округления:
  1. **Математическое** (стандартное) — ближайшее.
  2. **Бухгалтерское (bankers rounding)** — к ближайшему чётному при .5.
  3. **Усечение** — отбрасывание дробной части.
- Интерфейс: табличное представление результатов.

---

## 🚀 Быстрый старт

### Требования

- Node.js (рекомендуется LTS)
- npm
- Angular CLI (опционально): `npm install -g @angular/cli`

### Установка и запуск

```bash
# 1. Клонировать репозиторий
git clone https://github.com/ВАШ_ЛОГИН/ВАШ_РЕПОЗИТОРИЙ.git

# 2. Перейти в папку проекта
cd big-number-calculator

# 3. Установить зависимости
npm install

# 4. Запустить сервер разработки
# если установлен Angular CLI
ng serve
# или через npm-скрипт
npm start

# 5. Открыть в браузере
# После успешной компиляции
# http://localhost:4200/
```

> Если `ng` не найден, используйте `npm start` или установите CLI глобально: `npm install -g @angular/cli`.
